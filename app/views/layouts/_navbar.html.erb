


<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <%= link_to 'Mɯatcher', root_path %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
    <%= link_to 'Home', root_path %>
      </li>
      <li class="nav-item">
    <%= link_to 'Movies', movies_path %>
      </li>
      <% if user_signed_in? %>
        <%= link_to "Profile", user_profile_path %>
      <% end %>
    </ul>

        <div class="sing-in">
        <% if user_signed_in? %>
          <%# <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"> %>
          <%= link_to 'Sing Out', destroy_user_session_path, method: :delete, class:'btn btn-outline-dark button', style:'margin-left: 5px' %>
        <% else %>
          <%= link_to 'Sing Up', new_user_registration_path, class:'btn btn-outline-dark button' %>
          <%= link_to 'Sing In', new_user_session_path, class:'btn btn-outline-dark button', style:'margin-left: 5px' %>
        <% end %>
     </div>
  </div>

</nav>